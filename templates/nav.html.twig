<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
  <div class="container-fluid">
    <a class="navbar-brand keychainify-checked fw-bold" href="{{path('homepage')}}">ProtoEcom</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarColor01">
      <ul class="navbar-nav me-auto">

        {% for c in categoryRepository.findAll() %}
          <li class="nav-item">
            <a class="nav-link" href="{{path('product_category', {'slug': c.slug})}}">{{c.name}}</a>
          </li>
        {% endfor %}

      </ul>

      <ul class="navbar-nav">
        <li class="nav-item"><a href="#" class="nav-link">Inscription</a></li>
        <li class="nav-item"><a href="#" class="btn btn-outline-success">Connexion</a></li>
        <li class="nav-item"><a href="#" class="btn btn-outline-danger">Déconnexion</a></li>
      </ul>
    </div>
  </div>
</nav>
